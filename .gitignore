build/
*.o
*.d
*.exe
*.pdf

# Personal release tooling and its output: the published artifacts are
# attached to GitHub releases, not committed, and the script that builds
# them is local workflow rather than part of the project.
releases/
release.ps1

# Reference output captured before the cross-platform migration, used to prove
# the refactor changed no bytes. Local workflow, not part of the project.
.baseline/

# aqtinstall's log, left behind when Qt is fetched into a home directory.
aqtinstall.log
